# Nothing is built into this crate directory any more; packages/sigmakee owns
# the wasm output. These cover stray local wasm-pack runs and the output of the
# retired build-npm.sh / serve.sh / build-vampire.sh, which an older checkout
# may still have on disk (the Vampire build tree is GB-scale).
/pkg/
/pkg-*/
node_modules/
/.vampire-cache/
/web/
