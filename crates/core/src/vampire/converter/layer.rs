// crates/core/src/vampire/converter/layer.rs
//
// Reserved for a future logic-conversion abstraction over the
// vampire converter pipeline.  The struct is a stub today; the
// `#[allow(dead_code)]` keeps default builds warning-clean until it
// gains real fields and a constructor.

#[allow(dead_code)]
struct LogicConversionLayer {}
